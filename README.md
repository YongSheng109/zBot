# zBot
My first project in python - BOTTER AND DDOS ATTACK - OVH, TCP, UDP

```
sudo wget https://github.com/YongSheng109/zBot/raw/main/bot.zip
sudo unzip bot.zip
sudo cd bot
sudo apt update -y
sudo apt install tmux -y
sudo apt install nodejs -y
sudo apt install npm -y
sudo apt install python3 -y
sudo apt install pip -y
sudo pip install discord
sudo chmod +x *
sudo chmod +x files/*
sudo chmod +x methods/*
sudo chmod +x methods/L4/*
sudo chmod +x methods/L7/*
sudo chmod +x methods/OVH/*
sudo wget https://ftp.lysator.liu.se/pub/opensuse/repositories/network:/utilities/openSUSE_Leap_15.3/x86_64/paping-1.5.5.git1452081947.1ef470a-lp153.1.1.x86_64.rpm && sudo apt install rpm2cpio -y && sudo rpm2cpio paping-1.5.5.git1452081947.1ef470a-lp153.1.1.x86_64.rpm | cpio -idmv
```
